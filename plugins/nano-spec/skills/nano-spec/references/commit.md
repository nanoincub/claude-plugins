# Commit

> **Conteúdo movido.** O fluxo completo de commit (Conventional Commits + gates obrigatórios
> + rastreabilidade + integração com `superpowers:verification-before-completion` e
> `superpowers:finishing-a-development-branch` + 4 opções de fechamento incluindo discard)
> agora vive na skill [`nano-spec:nano-commit`](../../nano-commit/SKILL.md).
>
> **Como usar:**
> - O orquestrador `nano-spec` invoca `Skill('nano-spec:nano-commit')` na fase Commit.
> - Para invocar standalone (só commitar, sem entrar no fluxo completo de spec), basta
>   pedir "commitar", "fazer commit", "criar PR", etc. — a skill é auto-disparada via description.
>
> Este pointer existe apenas para compatibilidade com links antigos do orquestrador.
